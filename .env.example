# Cloud sync via Clerk (optional).
# Get a Publishable Key from https://dashboard.clerk.com → your app → API Keys.
# Copy this file to ".env.local" and paste your key. Without it, the app runs
# fine using local (per-browser) progress only.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxx
